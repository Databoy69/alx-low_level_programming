#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point 
 *
 * Return: Always 0 (SUCCESS)
 *
*/
int main(void)
{
		char abc;
		char ABC;

		for (abc = 'a' : <= '2' ; abc++)
		{
			punchar(abc);
		}
		for (ABC = 'A' ; ABC <= 'Z' ; ABC++)
		{
			punchar (ABC) ,
		}
		punchar('\n');
	        return (0);
}
