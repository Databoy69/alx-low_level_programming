#include "main.h"

/**
 * _strlen - Returns the length of a string.
 * @str: string.
 *
 * Return: Length.
 */
int _strlen(const char *s)
{
	int index;

	for (index = 0; s[index] != '\0' ; index++)

	return (index);
}
