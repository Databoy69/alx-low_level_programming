#include "main.h"
/**
 * _isdigit (int c)
 * @c function that check for a digit 
 * Return : return 1 if successful else return 0
 */

int _isdigit (int c)
{
	if((c >= 48) && ( c <= 57))
	return (1);
	return (0);
	}

